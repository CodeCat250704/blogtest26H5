JASPERBLOG/
├── anit-debugging/
│   └── anit.js
├── data-base/
│   ├── article/            (目录)
│   ├── modular/            (目录)
│   ├── music/              (目录)
│   ├── notice/             (目录)
│   ├── video/              (目录)
│   └── catalog.json        (核心数据清单)
├── modular-computer/
│   ├── about/              (目录)
│   ├── classification/     (目录)
│   ├── collection/         (目录)
│   ├── home               (完整模块)
│   │   ├── carousel       (目录)
│   │   │   ├── index.html
│   │   │   ├── script.js
│   │   │   └── style.css
│   │   ├── history        (目录)
│   │   │   ├── index.html
│   │   │   ├── script.js
│   │   │   └── style.css
│   │   ├── music          (目录)
│   │   │   ├── index.html
│   │   │   ├── script.js
│   │   │   └── style.css
│   │   ├── notice         (目录)
│   │   │   ├── index.html
│   │   │   ├── script.js
│   │   │   └── style.css
│   │   ├── index.html
│   │   ├── script.js
│   │   └── style.css
│   ├── setting/            (目录)
│   └─togrther/          (目录)
├── modular-pad/        (目录)
├── modular-phone/      (目录)
├── modular-watch/      (目录)
├ overall-situation
│       ├── right-click    (右键菜单子模块)
│       │   ├── index.html
│       │   ├── script.js
│       │   └── style.css
│       ├── taskbar        (任务栏子模块)
│       │   ├── index.html
│       │   ├── script.js
│       │   └── style.css
│       ├── equipment.js
│       ├── overall.css
│       ├── index.html
│       └── overall.js
└── index.js              (根目录下的index，只有框架，引入合并其余其他模块的index)






/* ==========================================================
 * 全系统亮暗主题自适应控制中心 - 完美变色版
 * ========================================================== */

/* 1. 默认主题：暗色模式 */
:root {
    /* 字体颜色 */
    --text-main: #ffffff;
    --text-muted: rgba(255, 255, 255, 0.7);
    --text-light: rgba(255, 255, 255, 0.4);
    --text-contrast: #4F9CF7; /* 品牌蓝 */
    
    /* 背景与毛玻璃 */
    --bg-card: rgba(255, 255, 255, 0.05);
    --bg-taskbar: rgba(16, 20, 30, 0.60); /* 暗色玻璃 */
    --bg-overlay: rgba(0, 0, 0, 0.4);
    --border-color: rgba(79, 156, 247, 0.3);
    --shadow-color: rgba(0, 0, 0, 0.5);
}

/* 2. 亮色模式变量重写 (字体变黑，背景变透) */
body.light-mode {
    --text-main: #222222;          /* 核心文字变深灰 */
    --text-muted: rgba(50, 50, 50, 0.7);
    --text-light: rgba(50, 50, 50, 0.5);
    
    --bg-card: rgba(255, 255, 255, 0.30);
    --bg-taskbar: rgba(255, 255, 255, 0.55); /* 亮色毛玻璃 */
    --bg-overlay: rgba(255, 255, 255, 0.1);
    --border-color: rgba(79, 156, 247, 0.5);
    --shadow-color: rgba(0, 0, 0, 0.08);
}

/* 3. 基础重置与容器 */
body, html {
    margin: 0; padding: 0; overflow: hidden;
    font-family: -apple-system, sans-serif;
    color: var(--text-main);
    background-color: #1a1a1a; /* 兜底背景 */
}

#root, #app-module-container {
    color: var(--text-main);
}

/* 4. 桌面壁纸 */
.desktop-wallpaper {
    position: absolute; top: 0; left: 0; width: 100%; height: 100%;
    background-image: url('./background.png'); background-size: cover; background-position: center; z-index: 0;
}

/* 5. 毛玻璃面板标准模板 (用于统一各级卡片) */
.glass-panel, .glass-effect, .settings-card, .submit-card, .pc-submit-section, .pad-submit-section, .phone-submit-section, .watch-submit-section, .category-btn, .article-item, .about-card, .taskbar-item {
    background: var(--bg-card) !important;
    border: 1px solid var(--border-color) !important;
    backdrop-filter: blur(12px);
    -webkit-backdrop-filter: blur(12px);
    color: var(--text-main) !important;
    box-shadow: 0 4px 15px var(--shadow-color);
}

/* 6. 字体颜色强制覆盖 (打底) */
h1, h2, h3, h4, h5, h6, p, span, div, a, li, i, button, input, textarea, .taskbar-item, .t-item, .mode-btn, .topbar-left, .topbar-right, .taskbar-left, .taskbar-right, .taskbar-center {
    color: var(--text-main) !important;
    transition: color 0.3s ease, background 0.3s ease;
}

.text-muted, .desc, .subtitle, input::placeholder {
    color: var(--text-muted) !important;
}
.brand-text {
    color: var(--text-contrast) !important;
}

/* 7. 交互状态 (鼠标悬停及激活) */
.t-item:hover, .taskbar-item:hover, .mode-btn:hover, .category-btn:hover, .article-item:hover {
    background: rgba(255, 255, 255, 0.08) !important;
    color: var(--text-main) !important;
}
.t-item.active, .taskbar-item.active, .mode-btn.active {
    background: rgba(79, 156, 247, 0.15) !important;
    border: 1px solid rgba(79, 156, 247, 0.2) !important;
    color: var(--text-contrast) !important;
}


/* ==========================================================
 * 【核弹级清除】：强制抹除所有残留的顶部黑块
 * ========================================================== */
#global-mode-switcher,
#global-mode-switcher .topbar-left,
#global-mode-switcher .topbar-center,
#global-mode-switcher .topbar-right,
#global-mode-switcher .mode-btn,
#global-mode-switcher > div {
    background: transparent !important;
    border: none !important;
    border-bottom: none !important;
    box-shadow: none !important;
}

/* 彻底覆盖任何可能带背景或边框的幽灵元素 */
#global-mode-switcher .switcher-panel {
    background: var(--bg-taskbar) !important;
    border: 1px solid var(--border-color) !important;
}

/* 特别针对你截图中那个带有 "JASPER Blog" 文字的残留块进行物理删除 */
.topbar-left, .topbar-left span, .topbar-left i, .topbar-left div {
    background: transparent !important;
    border: none !important;
    color: var(--text-main) !important;
}

/* 如果旧 HTML 里的 JASPER Blog 文字块还没彻底干掉，这里确保它彻底透明 */
div[class*="topbar-left"], div[class*="JASPER"] {
    background: transparent !important;
}